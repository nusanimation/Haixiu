using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Research.Kinect.Nui;

namespace WpfApplication1
{/*
    class coordCalc
    {
        public enum bodyGroups{
            Head, NeckV, HandL, HandR, Core, LegV, LegR
        }

        private SkeletonData data;
        public void coordCalc(SkeletonData r)
        {
            this.data = r;
        }
        public Vector getRelativePosition(bodyGroups g){
            Vector v;
            switch (g)
            {
                case bodyGroups.Head: calHead();  break;
            }
            return v;
        }
        private Vector calHead() 
        {
            return data.Joints[JointID.Head].Position;
        }

        private Vector calNeck()
        {
            return data.Joints[JointID.ShoulderCenter].Position;
        }
    }

*/
}